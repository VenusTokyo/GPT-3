import React from "react";
import ReactDOM from "react-dom";
import App from "./App"
import './index.css'
ReactDOM.render(<App/>,document.getElementById('root'))
//in the above we are saying that render the app component inside the div container with id root, that div container is the only thing in the body of index.html in the public folder